language: php
php:
  - "5.5"
  - "5.6"
  - "7.0"
  - "hhvm"

before_script:
  - composer self-update
  - composer install
script:
  - composer run test

cache:
  directories:
    - vendor

notifications:
  on_success: never
  on_failure: never